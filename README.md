
para hacer el /build:
     `meson setup build`

para compilar:
     `meson compile -C build`

para ejecutar el cliente :
     `./build/cliente `

para ejecutar el servidor :
     `./build/servidor `

